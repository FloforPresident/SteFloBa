using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollectDrop : MonoBehaviour {

    public GameObject drop1;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //var spaceship = gameObject.GetComponent<Rigidbody2D>();
        //Vector2 spaceshipPos = new Vector2(spaceship.transform.position.x, spaceship.transform.position.y);
        //Vector2 dropPos = new Vector2(drop1.transform.position.x, drop1.transform.position.x);
        
        //if (spaceshipPos == dropPos)
        //{
        //    GameControlScript.health += 1;
        //}
	}
}
